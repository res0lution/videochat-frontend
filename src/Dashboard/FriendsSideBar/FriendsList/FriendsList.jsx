import { styled } from "@mui/system"

const MainContainer = styled('div')({
  flexGrow: 1,
  width: '100%',
})

const FriendsList = () => {
  return (
    <MainContainer>
      <div></div>
    </MainContainer>
  )
}

export default FriendsList